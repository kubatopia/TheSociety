---
interface Props {
  title: string;
  lede?: string;
}
const { title, lede } = Astro.props;
---

<div class="mb-10 max-w-2xl">
  <h1 class="font-serif text-3xl font-semibold tracking-tight sm:text-4xl">{title}</h1>
  {lede && <p class="mt-3 text-lg text-ink-muted">{lede}</p>}
</div>
